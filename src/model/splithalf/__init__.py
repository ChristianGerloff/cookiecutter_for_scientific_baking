"""split half analysis module

This module allows the perform a split half analysis.

This script requires that `pandas`, `numpy` and `scipy` is
installed within you environment.

"""
from .splithalf import simple_split_half
from .pearson_brown import *